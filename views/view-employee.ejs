<% include partials/header.ejs %>
<div class="page-wrapper">
    <% if (employee) {%>
        <table class="table table-sm">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">     </th>
                    <th scope="col">     </th>
                    <th scope="col">     </th>
                    <th scope="col">     </th>
                    <th scope="col">     </th>
                    <th scope="col">     </th>
                    <th scope="col">System ID</th> <!-- biostar2_ac.t_usr.usruid! --> 
                    <th scope="col">För- & efternamn</th>
                    <th scope="col">Personnummer</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>     </td>
                    <td>     </td>
                    <td>     </td>
                    <td>     </td>
                    <td>     </td>
                    <td>     </td>
                    <td><%= employee.USRUID %></td>
                    <td><%= employee.NM %></td>
                    <td><%= personnummer %></td><!-- personnummer -->
                </tr>
            </tbody>
        </table>
    <% } else { %>
        <p class="text-center">An error has occurd. Please contact system administrator. <a href="/"> Go back to employee list</a></p>
    <% } %>

    <% if (timecard.length) { %>
        <div class="container">
        <form>
            <label>
                From:
                <input name="start" type="date" class="form-control"></input>
            </label>
            <label>
                To:
                <input name="end" type="date" class="form-control"></input>
            </label>
            <button type="submit" class="btn btn-sm btn-success">📆 Filtrera Vy</button>
            <button class="btn btn-sm btn-primary" onclick="window.print();return false;">🖨️ Skriv ut Vy</button>
        </form>
        <table class="table table-sm table-striped">
        <thead class="thead-dark">
            <tr>
                <th scope="col">Datum</th>
                <th scope="col">Tid in</th>
                <th scope="col">Tid ut</th>
            </tr>
        </thead>
        <tbody>
            <% timecard.forEach((timecard, index) => { %>
                <tr>
                    <td><%= timecard.date %></td>
                    <td><%= timecard.punch_in ? moment(timecard.punch_in).format('HH:mm') : 'N/A' %></td>
                    <td><%= timecard.punch_out ? moment(timecard.punch_out).format('HH:mm') : 'N/A' %></td>
                </tr>
            <% }) %>
        </tbody>
        </table>
        </div>
    <% } else { %>
        <p class="text-center">No Data Found. Click <a href="/">here</a> to get back to homepage.</p>
    <% } %>
</div>
</div>
</body>
</html>