<% include partials/header.ejs %>
    <div class="table table-wrapper">
        <% if (employees.length > 0) {%>
            <table class="table table-sm table-striped">
                <thead class="thead-dark">
                    <tr>
                        <th scope="col"></th>
                        <th scope="col"></th>
                        <th scope="col"></th>
                        <th scope="col">System ID</th>
                        <th scope="col">Namn</th>
                        <th scope="col">Konto skapat</th>
                        <th scope="col">Personnummer</th>
                        <th scope="col">Funktion</th>
                    </tr>
                </thead>
                <tbody>
                    <% employees.forEach((employees, index) => { %>
                        <tr>
                            <td></td>
                            <td></td>
                            <td></td>
                            <th scope="row"><%= employees.USRUID %></th>
                            <td><%= employees.NM %></td>
                            <td><%= moment(employees.CREATEDT).format('YYYY-MM-DD') %></td>
                            <td><%= personnummer[employees.USRUID] %></td> <!--personnummer[employees.USRUID]-->
                            <td>
                                <a href="/employee/<%= employees.USRUID %>" rel="noopener" class="btn btn-sm btn-success">Visa Tidskort</a>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } else { %>
            <p class="text-center">No Users found. Go <a href="/add" >here</a> to add Users.</p>
        <% } %>
    </div>
</div>
</body>
</html>
